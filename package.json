{
  "name": "bamis-blog",
  "version": "1.0.0",
  "description": "blog-bamis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index",
    "build": "esbuild index.js --bundle --minify --platform=node --outfile=dist/main.js --external:./database/*"
  },
  "author": "parna",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "esbuild": "^0.19.11",
    "express": "^4.18.2",
    "inversify": "^6.0.2",
    "mongoose": "^8.0.3",
    "nodemon": "^3.0.2",
    "reflect-metadata": "^0.2.1"
  }
}
